{
  "name": "backend",
  "version": "0.0.0",
  "type": "module",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@caido/sdk-backend": "^0.47.1",
    "shared": "workspace:*",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "zod": "^3.25.30"
  }
}
